---

# `api/README.md`

```markdown
# Módulo de Rutas / Endpoints (`api/`)

Esta carpeta contiene la definición de los endpoints de la aplicación enrutados mediante `APIRouter` de FastAPI.

- **`user_api.py`**: Maneja las operaciones CRUD de los usuarios (registro, autenticación, consulta y actualización), conectando las peticiones HTTP con la lógica de base de datos y esquemas de validación.
```
